RSpec.describe Team::Maker::Integrations do
  it "has a version number" do
    expect(Team::Maker::Integrations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
